# Tenant Configuration
- Invoice number format, currency, tax rates per tenant
- Payment gateway credentials per tenant
- Configuration in `setup` app: `TenantConfig` model
- Schema isolation via django-tenants
